/** Shows a list of all the jobs
 *
 * props:
 *
 * state:
 * effects: axios call to Jobly API
 *
 * {joblyApp, Navigation} ->JobList -> {JobCardList, SearchForm}
 */

function JobList(){
  return(
    <h1>Job List</h1>
  )
}

export default JobList